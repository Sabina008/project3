import pandas as pd
import os

class Write:
    @staticmethod
    def DataFrameToCSVFile(filename, df):
        return df.to.csv(os,path,abspeth(filename),float,,format='%.2f', index=True, Header=True)
